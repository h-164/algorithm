function solution(price, money, count) {
    var answer = 0;
    
    for(let i = 1; i <= count; i++){
        answer += price*i
    }
    
    money - answer >= 0? answer = 0 : answer = answer - money

    return answer;
}
