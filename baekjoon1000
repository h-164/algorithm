const fs = require("fs");
let input = fs.readFileSync("/dev/stdin").toString().split();//split을 쓰면 배열로 만들어짐
input = input[0];
input = input.split(" ").map((item) => +item);

solution(input[0], input[1]);

function solution(A, B) {
    console.log(A + B);
}
